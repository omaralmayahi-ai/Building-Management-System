# 📖 دليل التنصيب والتشغيل على سيرفر محلي (Windows Server)
### وزارة النفط العراقية — شركة نفط الوسط (MDOC)
### منظومة السجل الرقمي الموحد للأصول الهندسية والإنشائية

---

## 📑 الفهرس
1. [المتطلبات الأساسية للنظام](#1-المتطلبات-الأساسية-للنظام)
2. [الخيار الأول: التنصيب والتشغيل على سيرفر متصل بالإنترنت](#2-الخيار-الأول-التنصيب-والتشغيل-على-سيرفر-متصل-بالإنترنت-online)
3. [الخيار الثاني: التنصيب والتشغيل في بيئة معزولة تماماً دون إنترنت (Air-Gapped / Offline)](#3-الخيار-الثاني-التنصيب-والتشغيل-في-بيئة-معزولة-تماما-دون-إنترنت-offline)
4. [التشغيل التلقائي عند إقلاع السيرفر (Windows Service / Task Scheduler)](#4-التشغيل-التلقائي-عند-إقلاع-السيرفر)
5. [إعدادات جدار الحماية (Windows Firewall) والوصول الشبكي](#5-إعدادات-جدار-الحماية-windows-firewall-والوصول-الشبكي)
6. [إعداد قاعدة البيانات وحفظ النسخ الاحتياطية](#6-إعداد-قاعدة-البيانات-وحفظ-النسخ-الاحتياطية)
7. [الأسئلة الشائعة واستكشاف الأخطاء](#7-الأسئلة-الشائعة-واستكشاف-الأخطاء)

---

## 1. المتطلبات الأساسية للنظام

| المتطلب | الحد الأدنى | الموصى به |
| :--- | :--- | :--- |
| **نظام التشغيل** | Windows Server 2016 / 2019 / 2022 أو Windows 10/11 Pro | Windows Server 2022 Datacenter |
| **المعالج (CPU)** | 2 Cores | 4 Cores أو أكثر |
| **الذاكرة العشوائية (RAM)** | 4 GB | 8 GB أو أكثر |
| **المساحة التخزينية** | 5 GB SSD | 20 GB SSD |
| **بيئة التشغيل (Runtime)** | Node.js v18 LTS أو v20 LTS | Node.js v20.x LTS |

> **ملاحظة:** تأكد من تثبيت **Node.js** عبر الموقع الرسمي [nodejs.org](https://nodejs.org) (تحميل مثبت `.msi`).

---

## 2. الخيار الأول: التنصيب والتشغيل على سيرفر متصل بالإنترنت (Online)

إذا كان سيرفر Windows Server متصلاً بالإنترنت:

1. **استنساخ المشروع من GitHub أو فك ضغط الملفات:**
   ```powershell
   git clone https://github.com/your-username/your-repo.git
   cd your-repo
   ```
2. **التشغيل المباشر بنقرة واحدة:**
   * اضغط بزر الفأرة الأيمن على الملف: `setup-online-windows.bat`
   * اختر **تشغيل (Run)**.
3. **ما الذي يفعله السكربت تلقائياً؟**
   * يفحص وجود `Node.js` في بيئة النظام.
   * يُنشئ ملف الإعدادات `.env` إن لم يكن موجوداً.
   * يقوم بتنزيل وتثبيت جميع المكتبات والاعتماديات البرمجية (`npm install`).
   * يقوم ببناء وتجميع واجهة الويب ومحرك السيرفر الموحد في مجلد `dist/`.
   * يُطلق السيرفر على المنفذ `3000`.
4. **رابط الدخول:**
   * من السيرفر نفسه: `http://localhost:3000`
   * من أي حاسبة في الشبكة: `http://[SERVER_IP]:3000`

---

## 3. الخيار الثاني: التنصيب والتشغيل في بيئة معزولة تماماً دون إنترنت (Offline / Air-Gapped)

إذا كان السيرفر في موقع نفطي أو مركز بيانات **معزول تماماً عن الإنترنت الخارجي**:

### الخطوة الأولى: التجهيز على حاسبة متصلة بالإنترنت (مرة واحدة فقط)
1. نزّل المشروع من GitHub على حاسبة عادية متصلة بالإنترنت.
2. شغّل ملف `prepare-offline-package.bat` (أو نفّذ الأوامر التالية):
   ```cmd
   npm install
   npm run build
   ```
3. سيقوم السكربت بتحميل كافة المكتبات إلى مجلد `node_modules` وبناء حزمة الإنتاج في مجلد `dist`.
4. انسخ مجلد المشروع بالكامل إلى **فلاشة USB** أو هارد خارجي.
5. انسخ أيضاً ملف تثبيت `Node.js (node-v20.x.x-x64.msi)` إلى الفلاشة.

### الخطوة الثانية: النقل والتشغيل على السيرفر المعزول
1. ثبّت برنامج `Node.js` على السيرفر المعزول عبر ملف الـ `.msi` المنقول بالفلاشة.
2. الصق مجلد المشروع في المسار المفضل على السيرفر (مثال: `C:\MDOC_Assets_System`).
3. اضغط مرتين على ملف:
   ```cmd
   setup-offline-windows.bat
   ```
   أو ملف التشغيل السريع:
   ```cmd
   start-server.bat
   ```
4. سيعمل النظام فوراً بكامل طاقته ومميزاته دون الحاجة لأي اتصال خارجي.

---

## 4. التشغيل التلقائي عند إقلاع السيرفر (Windows Service / Task Scheduler)

لضمان بقاء النظام يعمل 24/7 حتى لو أعيد تشغيل السيرفر أو لم يقم أحد بتسجيل الدخول:

### الطريقة الأولى: عبر سكربت PowerShell المرفق (تلقائي)
1. اضغط بزر الفأرة الأيمن على ملف `register-windows-service.ps1`.
2. اختر **Run with PowerShell** (أو شغّله من موجه الأوامر بصلاحيات Administrator).
3. سيقوم السكربت تلقائياً بـ:
   * فتح المنفذ 3000 في جدار الحماية (Windows Firewall).
   * تسجيل المنظومة كمهمة نظام في `Windows Task Scheduler` تعمل مع إقلاع الويندوز بصلاحيات `SYSTEM`.
   * استخراج وطباعة عناوين الـ IP الخاصة بالسيرفر للوصول المباشر.

### الطريقة الثانية: باستخدام أداة NSSM (Service Manager)
1. حمّل أداة `nssm.exe` وضعها في مجلد المشروع.
2. افتح موجه الأوامر كمسؤول ونفّذ:
   ```cmd
   nssm install MDOC_Assets_Service "C:\Program Files\nodejs\node.exe" "C:\MDOC_Assets_System\dist\server.cjs"
   nssm set MDOC_Assets_Service AppDirectory "C:\MDOC_Assets_System"
   nssm start MDOC_Assets_Service
   ```

---

## 5. إعدادات جدار الحماية (Windows Firewall) والوصول الشبكي

للسماح للحاسبات والأجهزة اللوحية داخل حقول ومواقع شركة نفط الوسط بالدخول للمنظومة:

1. افتح **PowerShell كمسؤول (Run as Administrator)**.
2. نفّذ الأمر التالي لفتح المنفذ `3000`:
   ```powershell
   New-NetFirewallRule -DisplayName "MDOC Assets System" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
   ```
3. لمعرفة عنوان الـ IP الخاص بالسيرفر لتوزيعه على المستخدمين:
   ```powershell
   ipconfig
   ```
4. عنوان الدخول سيكون: `http://192.168.x.x:3000` (استبدل بالأيبي الفعلي للسيرفر).

---

## 6. إعداد قاعدة البيانات وحفظ النسخ الاحتياطية

### 1. التخزين المحلي التلقائي (Zero-Config JSON Store)
* المنظومة مهيأة تلقائياً للحفظ الفوري والتحديث التلقائي في ملف:
  `data_store_backup.json`
* يتم حفظ جميع الأصول، وسجلات الصيانة، والإشغال، والجولات التفتيشية فوراً ومحلياً عند كل تعديل.
* **لأخذ نسخة احتياطية:** يكفي نسخ ملف `data_store_backup.json` إلى مكان آمن دورياً.

### 2. الربط مع قاعدة بيانات PostgreSQL مركزية (اختياري)
إذا رغبت بربط المنظومة بسيرفر PostgreSQL موجود في شبكة الشركة:
1. افتح ملف `.env`.
2. عيّن متغير الاتصال:
   ```env
   DATABASE_URL=postgres://oil_user:your_password@localhost:5432/midland_assets_db
   ```
3. ستقوم المنظومة بالتحويل التلقائي للقاعدة المركزية مع الحفاظ على التزامن الحي (Live SSE Sync).

---

## 7. الأسئلة الشائعة واستكشاف الأخطاء

* **س: هل تعمل الخرائط والمخططات الهندسية دون إنترنت؟**
  * **ج:** نعم! تم تضمين جميع أيقونات ومكتبات Leaflet ومحرر المخططات CAD محلياً داخل حزمة المشروع دون طلب أي مورد خارجي.

* **س: كيف أقوم بتغيير منفذ التشغيل من 3000 إلى منفذ آخر (مثلاً 80 أو 8080)؟**
  * **ج:** افتح ملف `.env` وعدّل السطر:
    ```env
    PORT=80
    ```
    ثم أعد تشغيل السيرفر.

* **س: ما هي بيانات الدخول الافتراضية؟**
  * **اسم المستخدم:** `admin`
  * **كلمة المرور:** `admin123`

---
**قسم تكنولوجيا المعلومات والاتصالات — شركة نفط الوسط (MDOC)**
